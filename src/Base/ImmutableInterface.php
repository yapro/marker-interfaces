<?php

namespace YaPro\MarkerInterfaces\Base;

/**
 * Иммутабельный - неизменяемый объект (это значит, что данные в этом объекте инициализируются один раз - в момент
 * создания объекта и более не меняются).
 *
 */
interface ImmutableInterface
{
}
